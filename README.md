# Pillars of Fate

Minimal React + Vite starter (JavaScript).

Run locally:

```powershell
npm install
npm run dev
```

Build:

```powershell
npm run build
npm run preview
```

Open the dev URL printed by Vite (usually http://localhost:5173).
